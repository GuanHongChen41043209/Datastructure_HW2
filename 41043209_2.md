# Homework2
41043209 陳冠宏
2026/05/04

---
## AdjacencyListGraph
```cpp
struct EdgeNode {
    int vertex;
    int weight;
};

class WeightedAdjacencyListGraph {
private:
    int n;
    int e;
    vector<vector<EdgeNode>> adjList;
};
```
## 初始化AdjacencyMatrix
```cpp
WeightedAdjacencyListGraph(int vertices) {
    n = vertices;
    e = 0;
    adjList.resize(n);
}
```
## 判斷是否是空的
```cpp
bool IsEmpty() const {
    return n == 0;
}<img width="230" height="999" alt="2_output" src="https://github.com/user-attachments/assets/17e311b0-ceff-4643-833b-73575ee56db8" />

```
## 回傳有幾個點
```cpp
int NumberOfVertices() const {
    return n;
}
```
## 回傳有幾個邊
```cpp
int NumberOfEdges() const {
    return e;
}
```
## 新增邊
```cpp
void InsertEdge(int u, int v, int weight) {
    if (u < 0 || u >= n || v < 0 || v >= n) {
        cout << "Invalid vertex!" << endl;
        return;
    }
    if (weight <= 0) {
        cout << "Weight must be positive!" << endl;
        return;
    }
    for (int i = 0; i < adjList[u].size(); i++) {
        if (adjList[u][i].vertex == v) {
            adjList[u][i].weight = weight;
            for (int j = 0; j < adjList[v].size(); j++) {
                if (adjList[v][j].vertex == u) {
                    adjList[v][j].weight = weight;
                    return;
                }
            }
        }
    }
    adjList[u].push_back({v, weight});
    adjList[v].push_back({u, weight});
    e++;
}
```
## 回傳這個邊是否存在
```cpp
bool ExistsEdge(int u, int v) const {
    if (u < 0 || u >= n || v < 0 || v >= n) {
        return false;
    }
    for (int i = 0; i < adjList[u].size(); i++) {
        if (adjList[u][i].vertex == v) {
            return true;
        }
    }
    return false;
}
```
## 計算點的degree
```cpp
int Degree(int u) const {
    if (u < 0 || u >= n) {
        cout << "Invalid vertex!" << endl;
        return -1;
    }
    return adjList[u].size();
}
```
## 刪除邊
```cpp
void DeleteEdge(int u, int v) {
    if (u < 0 || u >= n || v < 0 || v >= n) {
        cout << "Invalid vertex!" << endl;
        return;
    }
    bool found = false;
    for (int i = 0; i < adjList[u].size(); i++) {
        if (adjList[u][i].vertex == v) {
            adjList[u].erase(adjList[u].begin() + i);
            found = true;
            break;
        }
    }
    for (int i = 0; i < adjList[v].size(); i++) {
        if (adjList[v][i].vertex == u) {
            adjList[v].erase(adjList[v].begin() + i);
            break;
        }
    }
    if (found) {
        e--;
    }
}
```
## 新增點
```cpp
void InsertVertex() {
    adjList.push_back(vector<EdgeNode>());
    n++;
}
```
## 刪除點
```cpp
void DeleteVertex(int v) {
    if (v < 0 || v >= n) {
        cout << "Invalid vertex!" << endl;
        return;
    }
    e -= adjList[v].size();
    adjList.erase(adjList.begin() + v);
    n--;
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < adjList[i].size(); j++) {
            if (adjList[i][j].vertex == v) {
                adjList[i].erase(adjList[i].begin() + j);
                j--;
            }
            else if (adjList[i][j].vertex > v) {
                adjList[i][j].vertex--;
            }
        }
    }
}
```
## 輸出Adjacency List
```cpp
void PrintList() const {
    cout << "Adjacency List:" << endl;
    for (int i = 0; i < n; i++) {
        cout << i << ": ";
        for (int j = 0; j < adjList[i].size(); j++) {
            cout << "(" << adjList[i][j].vertex 
                << ", " << adjList[i][j].weight << ") ";
        }
        cout << endl;
    }
}
```
## 執行結果
<img width="230" height="999" alt="2_output" src="https://github.com/user-attachments/assets/94adbaed-2272-470d-ac4d-097740649fde" />
```
